{
  "name": "student-pass-fail-predictor",
  "private": true,
  "version": "0.1.0",
  "description": "Root package for Student Pass/Fail Predictor project",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run backend\"",
    "backend": "cd backend && FLASK_APP=app.py flask run --host=0.0.0.0",
    "build": "npm run build --prefix frontend",
    "deploy": "npm run deploy --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
